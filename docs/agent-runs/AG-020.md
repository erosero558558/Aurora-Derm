# AG-020 - Tech debt P2: strip de debug logs en build de producción

## Contexto

- Task: `AG-020`
- Objetivo: eliminar `console.log/debugger` en bundles de producción y endurecer regla de consola en CI.

## Cambios aplicados

### 1) Strip en compile-time (Rollup)

- Archivo: `rollup.config.mjs`
- Se agregó `@rollup/plugin-strip` con configuración:
    - funciones removidas: `console.log`, `console.debug`, `console.trace`
    - `debugger: true`
- Se definió `productionPlugins = [resolve(), stripDebug, minify]` y se aplicó a todos los bundles.

### 2) Regla no-console (warn local / error CI)

- Archivo: `eslint.config.js`
- Se agregó:
    - `const ciNoConsoleSeverity = process.env.CI ? 'error' : 'warn';`
    - override para `js/**/*.js` y `src/**/*.js`:
        - `no-console` con severidad dinámica
        - allowlist: `warn`, `error`, `info`

### 3) Gate de CI sobre bundles resultantes

- Archivo: `.github/workflows/ci.yml`
- En job `build`, después de generar bundle:
    - paso `Enforce no debug logs in production bundles`
    - falla si encuentra `console.log|console.debug|console.trace|debugger` en:
        - `script.js`
        - `js/engines`
        - `js/chunks`
        - `js/admin-chunks`

### 4) Dependencia nueva

- Archivos:
    - `package.json`
    - `package-lock.json`
- Se agregó `@rollup/plugin-strip`.

## Validación local

1. `npm install` -> OK
2. `npm run lint:js` -> OK (sin errores)
3. `npm run build` -> OK
4. Verificación de bundles:
    - `rg -n -e "console\\.(log|debug|trace)\\(" -e "\\bdebugger\\b" script.js js/engines js/chunks js/admin-chunks`
    - Resultado: sin hallazgos

## Resultado

AG-020 implementado con control en build y enforcement en CI para evitar fuga de debug logs a producción.
