# AG-233 - Canonicalizar auth mock en queue-operator spec

## Resumen

- Se canonicalizo el mock inline de `admin-auth.php` en `tests/queue-operator.spec.js`.
- El spec ahora importa el helper compartido `installLegacyAdminAuthMock`.
- El fixture `mockOperatorSurface` mantiene el mismo `csrfToken` del operador (`csrf_operator`) y deja de duplicar el payload basico de auth.
- No fue necesario tocar la logica del operador ni los asserts del spec.

## Archivos

- `tests/queue-operator.spec.js`

## Verificacion

1. `npx eslint tests/queue-operator.spec.js tests/helpers/admin-auth-mocks.js`
    - Resultado: `0` errores, `0` warnings.
2. `TEST_LOCAL_SERVER_PORT=8055 npx playwright test tests/queue-operator.spec.js --workers=1`
    - Resultado: `7/7` passing.

## Notas

- El cambio deja a `tests/admin-openclaw-login.spec.js` como el unico frente restante con mock auth especial, porque modela el flujo `openclaw_chatgpt` y no un contrato legacy simple.
- `public_main_sync` seguia healthy durante el cierre de este bloque.
