# AG-221 - Align funnel tracking Playwright spec to current public V6 maintenance flow

## Summary

- Replaced the legacy checkout/chatbot assumptions in `tests/funnel-tracking.spec.js` with a contract for the current `public-v6` funnel routing.
- The spec now validates the maintained public entry flow:
    - `/en/` keeps the first-step guidance and search route alive.
    - `/en/services/acne-rosacea/` exposes the booking maintenance handoff to telemedicine.
    - `/en/telemedicine/` closes the loop back to `/en/services/`.
- Added explicit guards that the legacy booking shell is absent on these routes (`data-data-bundle`, `appointmentForm`, `paymentModal`, `chatbotWidget`).

## Reproduced issue

- `npx playwright test tests/funnel-tracking.spec.js --workers=1`
- Original failures timed out waiting for `script[data-data-bundle="true"]`, which no longer exists on the current `public-v6` routes.

## Verification

- `npx playwright test tests/funnel-tracking.spec.js --workers=1`
- `npx eslint tests/funnel-tracking.spec.js`

## Outcome

- `3/3` Playwright tests passing.
- ESLint clean for the updated spec.
