# CDX-003 - Package P2: shared PowerShell kernel for production ops scripts

Date: 2026-03-02
Owner: Ernesto
Executor: codex

## Scorecard

- Estado general: `GREEN`
- Proximo paquete: `desbloqueado` (`P3`)

## Metas comprometidas

- `cumplida` se extrajo el kernel comun a `bin/powershell/Common.Http.ps1`, `bin/powershell/Common.Metrics.ps1` y `bin/powershell/Common.Warnings.ps1`.
- `cumplida` `REPORTE-SEMANAL-PRODUCCION.ps1`, `VERIFICAR-DESPLIEGUE.ps1` y `MONITOR-PRODUCCION.ps1` quedaron como orquestadores con dot-sourcing de helpers compartidos.
- `cumplida` los flags y comandos de ejecucion via `npm` se conservaron.
- `cumplida` el modo advisory de `VERIFICAR-DESPLIEGUE.ps1` ahora tambien cubre checks frontend inline cuando el HEAD no toca frontend, evitando ruido ajeno al carril backend/ops.

## Baseline -> resultado

- `REPORTE-SEMANAL-PRODUCCION.ps1`: `2042` lineas -> `919`.
- `VERIFICAR-DESPLIEGUE.ps1`: `1968` lineas -> `1216`.
- `MONITOR-PRODUCCION.ps1`: `368` lineas -> `290`.
- Duplicacion compartida:
  HTTP/JSON, curl/hash, benchmark/Prometheus, warnings/ciclo semanal salieron de los scripts raiz y quedaron centralizados en `bin/powershell/*`.

## Contratos preservados

- Sin cambios breaking en `npm run verify:prod:fast`.
- Sin cambios breaking en `npm run report:weekly:prod`.
- Sin cambios breaking en `npm run monitor:prod`.
- `MONITOR-PRODUCCION.ps1` conserva parametros y salida operativa.
- `REPORTE-SEMANAL-PRODUCCION.ps1` sigue publicando `weeklyCycle` en markdown y JSON.

## Validacion

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -Command "[void][scriptblock]::Create((Get-Content -Raw 'REPORTE-SEMANAL-PRODUCCION.ps1')); [void][scriptblock]::Create((Get-Content -Raw 'VERIFICAR-DESPLIEGUE.ps1')); [void][scriptblock]::Create((Get-Content -Raw 'MONITOR-PRODUCCION.ps1')); [void][scriptblock]::Create((Get-Content -Raw 'bin/powershell/Common.Http.ps1')); [void][scriptblock]::Create((Get-Content -Raw 'bin/powershell/Common.Metrics.ps1')); [void][scriptblock]::Create((Get-Content -Raw 'bin/powershell/Common.Warnings.ps1'))"
node --test tests-node/weekly-report-script-contract.test.js tests-node/nightly-stability-workflow-contract.test.js tests-node/post-deploy-fast-workflow-contract.test.js tests-node/post-deploy-gate-workflow-contract.test.js
npm run verify:prod:fast
npm run report:weekly:prod
npm run monitor:prod
```

Resultado observado:

- Parseo PowerShell: `OK`.
- Contratos Node: `15 passed`, `0 failed`.
- `npm run verify:prod:fast`: `OK`; el hallazgo `index-inline-executable-script` queda en modo advisory cuando no hay cambios frontend.
- `npm run report:weekly:prod`: `OK`; artefactos generados en `verification/weekly/weekly-report-20260302.{md,json}`.
- `npm run monitor:prod`: `OK`.

## Riesgo residual

- `VERIFICAR-DESPLIEGUE.ps1` sigue reportando warnings advisory frontend (`initGA4`, `payment_success`, etc.) cuando el sitio remoto diverge pero el HEAD no toca frontend; eso ya no bloquea el carril backend/ops, pero sigue siendo señal operativa.
- `bin/powershell/Common.Http.ps1` concentra bastante logica de deploy/monitor; `P2` cerro el desacoplamiento de scripts, pero no divide aun ese kernel por subdominios mas finos.
