# AG-123 - Turnero sala X: onboarding guiado + práctica asistida en admin

Fecha de cierre: 2026-02-27  
Owner: Ernesto  
Executor: codex

## Alcance ejecutado

- `src/apps/admin/index.js`
    - Implementé una capa de práctica guiada para `#queue` sin tocar backend:
        - Definición canónica de pasos (`QUEUE_PRACTICE_STEPS`).
        - Estado de práctica (`practiceState`) con progreso, tiempo y acciones.
        - Panel dinámico `queuePracticeCoachPanel` inyectado por JS con:
            - progreso `x/4`
            - checklist de pasos completados
            - tiempo transcurrido
            - botón `Reiniciar práctica`.
    - Añadí estilos inline dedicados (`queuePracticeCoachInlineStyles`) para el coach.
    - Integré registro de pasos reales en modo práctica:
        - `Numpad Enter` -> `call_next`
        - `Numpad +` -> `re_llamar`
        - `Numpad .` -> `completar`
        - `Numpad -` -> `no_show`
    - Añadí `resetQueuePracticeProgress(...)` y acción UI `queue-reset-practice`.
    - Mejoré ciclo de vida:
        - `queue-start-practice` ahora marca onboarding como visto (`persist: true`).
        - limpieza de ticker/estado de práctica al salir a login.

## Validación ejecutada

1. `npx eslint src/apps/admin/index.js`  
   Resultado: OK.
2. `npx playwright test tests/admin-queue.spec.js`  
   Resultado: 19/20 OK; 1 fallo en test de triage (`A-900`) no relacionado al flujo de práctica guiada.
3. `npx playwright test tests/admin-queue.spec.js -g "modo práctica simula acciones"`  
   Resultado: 1 passed.
4. `npx rollup -c`  
   Resultado: build OK, `admin.js` regenerado.

## Resultado operativo

- Recepción tiene ahora entrenamiento guiado dentro del mismo panel admin.
- El modo práctica queda visible, medible y reiniciable sin afectar cola real.
- Se mantiene compatibilidad con atajos existentes y modo estación bloqueada.
