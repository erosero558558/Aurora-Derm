# CDX-042 - Admin shell core daily-use: frontend shell lane

Estado: `done`
Inicio: `2026-03-14`
Estrategia: `STRAT-2026-03-admin-operativo`
Subfrente: `SF-frontend-admin-operativo`

## Objetivo

- Hacer visible y usable a diario el shell `sony_v3`.
- Mantener login/shell sin superficies legacy rotas.
- Cerrar `dashboard`, `agenda`, `pendientes`, `horarios` e `historia clinica` con navegacion, command palette, responsive tablet y estados vacios estables.

## Archivos reservados

- `admin.html`
- `src/apps/admin-v3/index.js`
- `src/apps/admin-v3/core`
- `src/apps/admin-v3/shared`
- `src/apps/admin-v3/ui`
- `src/apps/admin-v3/sections`

## Entrega visible del RC1

- Shell visible reducido exactamente a `dashboard`, `appointments`, `callbacks`, `availability` y `clinical-history`.
- `queue/turnero` y `reviews` quedaron fuera de sidebar, hash routing, command palette, shortcuts y side effects de boot.
- El codigo de `queue` sigue en disco, pero no es alcanzable desde el shell RC1.

## Comandos ejecutados

- `npm run test:admin:runtime-smoke`
- `npx playwright test tests/admin-v3-shell.spec.js tests/admin-openclaw-login.spec.js tests/admin-navigation-responsive.spec.js tests/admin-callbacks-triage.spec.js tests/admin-availability-responsive.spec.js tests/admin-availability-readonly.spec.js tests/admin-quick-nav.spec.js --workers=1`
- `npm run gate:admin:rollout:internal`

## Resultado

- Las suites del shell RC1 pasaron en verde (`28/28` Playwright del corte visible y `2/2` runtime smoke).
- El login OpenClaw, la navegacion por sidebar/hash, la command palette y el responsive tablet quedaron cubiertos por tests reales.
- El shell ya no expone acciones hacia `queue/turnero` ni `reviews`.

## Deuda diferida a ola 2

- Reactivar `queue/turnero` solo cuando exista un plan dedicado y validado como segunda ola.
- Revisar si `queue-ops.css` sigue siendo necesario en el shell final o si conviene desacoplarlo en la siguiente limpieza visual.
