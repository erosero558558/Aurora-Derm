# AG-092 — Frontend admin: corregir mojibake en callbacks triage

Fecha: 2026-02-26
Owner: Ernesto
Executor: codex

## Cambios

- `src/apps/admin/modules/callbacks.js`
    - Correccion de textos mojibake en panel de callbacks:
        - `recién`
        - `Cola: atención requerida`
        - `Sin teléfono`
- `admin.js`
    - Paridad de runtime con el fix anterior para evitar divergencia entre source y bundle servido en `admin.html`.
- `tests/admin.spec.js`
    - Endurecimiento del test de triage de callbacks (`queueHealth` exacto para caso urgente).
    - Nuevo test de regresion UTF-8 para caso `atención requerida` + fallback `Sin teléfono`.
    - Guardrail anti-mojibake (`not.toContainText(/[ÃÂ]/)`).

## Validacion

- `npx playwright test tests/admin.spec.js --grep "callbacks triage"`
    - Resultado: 2 passed.
- `npm run test:frontend:qa:admin`
    - Resultado: 30 passed, 1 skipped.
- `npx eslint src/apps/admin/modules/callbacks.js tests/admin.spec.js`
    - Resultado: OK.

## Resultado

Bloque cerrado con fix visible en UI admin y regresion cubierta en QA.
