# AG-218 - Auto-close stale deploy alerts when public sync recovers

## Summary

- Added canonical recovery evaluation to `.github/workflows/prod-monitor.yml` so stale deploy-route production alerts auto-close only when `public_main_sync` is truly healthy.
- Removed the self-referential `operationallyHealthy` dependency from the recovery gate; closure now depends on host health, repo hygiene, no head drift, no telemetry gap, and zero dirty paths.
- Reduced `workflow_dispatch` inputs to stay within GitHub's 25-input limit and preserved contract coverage for the recovery flow.

## Files

- `.github/workflows/prod-monitor.yml`
- `tests-node/prod-monitor-workflow-contract.test.js`
- `docs/PUBLIC_MAIN_UPDATE_RUNBOOK.md`

## Verification

### Local

- `NODE_PATH=C:\Users\Ernesto\Documents\GitHub\piel-en-armonia\node_modules node --test tests-node/prod-monitor-workflow-contract.test.js tests-node/prod-monitor-public-sync-contract.test.js`
- Result: `20/20` tests passing.

### Pushed commits

- `8f222a7c` `fix prod monitor stale deploy alert recovery`
- `6240b7a6` `fix prod monitor workflow dispatch limit`
- `95d47e0d` `fix prod monitor public sync recovery gating`

### Live runs

- `23019285939` failed as expected while `public_sync_recovery` still saw `head_drift` (`8f222a7c` vs `6240b7a6`).
- `23019362090` exposed the self-referential recovery bug: `public_main_sync` was healthy, but stale deploy alerts kept `operationallyHealthy=false`, so the close step skipped.
- `23019569828` executed the stale-alert close step successfully and closed issues `#423`, `#424`, and `#426`.
- `23019627958` passed end-to-end after the stale alerts were closed, confirming the loop was fixed.

### Current production state

- `node agent-orchestrator.js jobs verify public_main_sync --json`
- Result on `2026-03-12`: `ok=true`, `healthy=true`, `state=idle`, `current_head=remote_head=b5e90dad11a0d2659143b165eec350b2d19dc9c0`, `dirty_paths_count=0`, `telemetry_gap=false`.
- `gh issue list --search "public_main_sync OR repair git sync OR connectivity deploy host in:title state:open"` returned `[]`.

## Outcome

- Production monitoring now auto-heals stale deploy-route alerts once `public_main_sync` is canonically healthy again.
- The stale incident loop is closed in both workflow behavior and current live state.
