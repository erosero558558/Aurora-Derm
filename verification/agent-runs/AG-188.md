# AG-188 - OpenClaw shared-session cross-surface spec

## Scope
- Agregar una prueba Playwright dedicada a la sesion compartida OpenClaw entre `admin.html` y `operador-turnos.html`.
- Validar reutilizacion cruzada de sesion y logout invalidando ambas superficies.

## Changes
- Se creo `tests/operator-auth-shared-session.spec.js`.
- El spec instala mocks compartidos de `admin-auth.php` y `api.php` a nivel de `browser context`.
- Se cubrieron dos recorridos:
  - admin autentica y operador reutiliza la misma sesion;
  - operador autentica, admin reutiliza y logout invalida ambos lados.

## Verification
- `TEST_REUSE_EXISTING_SERVER=1 npx playwright test tests/operator-auth-shared-session.spec.js --workers=1`
- Resultado: `2 passed`

## Notes
- No se hizo `git pull` en `main`; el worktree sigue sucio y remoto va por delante.
- El warning global vigente sigue siendo `public_main_sync unhealthy`.
