# AG-153 - FE-V6-R8 normalize V6 test helpers and naming

Date: 2026-03-09
Owner: ernes
Lane: frontend_content

## Summary

- Added `tests/helpers/public-v6.js` as the canonical helper for V6 public specs.
- Reduced `tests/helpers/public-v3.js` to a legacy wrapper for shared routines plus booking-hook compatibility.
- Migrated every `tests/public-v6-*.spec.js` import from `./helpers/public-v3` to `./helpers/public-v6`.
- Added `tests-node/public-v6-test-helper-contract.test.js` so V6 specs fail if they regress to the legacy helper.
- Extended `test:frontend:qa:v6` to include the new helper contract.

## Validation

Command:

```powershell
npm run test:frontend:qa:v6
```

Result:

- `32` Playwright specs passed.
- `2` helper contract tests passed.
- `4` V6 copy contract tests passed.

## Notes

- No admin files were touched.
- Legacy V3/V4/V5 suites still use `tests/helpers/public-v3.js`; this block only normalized the V6 QA surface.
