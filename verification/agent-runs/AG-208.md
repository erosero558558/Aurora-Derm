# AG-208 - Create transport preflight incident in deploy-hosting

## Summary

- Added a dedicated production incident path in `deploy-hosting.yml` for transport preflight failures classified as `runner_tcp_unreachable`.
- Kept the signal separate from telemedicine health so runner-to-host reachability can be triaged and auto-closed independently.
- Updated the public main update runbook to document the new incident lifecycle.

## Files

- `.github/workflows/deploy-hosting.yml`
- `tests-node/deploy-hosting-workflow-contract.test.js`
- `docs/PUBLIC_MAIN_UPDATE_RUNBOOK.md`

## Verification

- `node --test tests-node/deploy-hosting-workflow-contract.test.js`

## Result

- Contract suite passed: `8/8`.
- The workflow now creates or updates `[ALERTA PROD] Deploy Hosting transporte bloqueado desde GitHub Runner` when `transport_preflight_reason=runner_tcp_unreachable`.
- The workflow closes that incident automatically when a later transport preflight reports `transport_preflight_reason=ok`.
- This block does not change live host state by itself; it improves incident visibility for the existing transport/network failure mode.
