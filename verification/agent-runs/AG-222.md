# AG-222 - Align booking Playwright spec to current public V6 maintenance flow

## Summary

- Replaced the legacy appointment-form expectations in `tests/booking.spec.js` with the current Spanish `public-v6` maintenance contract.
- The spec now validates the maintained Spanish booking guidance flow:
    - `/es/` keeps first-step guidance, search routing, and booking maintenance CTA intact.
    - `/es/servicios/acne-rosacea/` exposes the booking-status handoff to telemedicine instead of the removed legacy shell.
    - `/es/telemedicina/` loops back to `/es/servicios/` while online booking remains paused.
- Added explicit guards that the legacy booking shell is absent on these routes (`data-data-bundle`, `appointmentForm`, `paymentModal`, `chatbotWidget`).

## Reproduced issue

- `npx playwright test tests/booking.spec.js --workers=1`
- Original failures timed out waiting for `#appointmentForm` and related legacy form controls that no longer exist on the current `public-v6` routes.

## Verification

- `npx playwright test tests/booking.spec.js --workers=1`
- `npx eslint tests/booking.spec.js`

## Outcome

- `3/3` Playwright tests passing.
- ESLint clean for the updated spec.
