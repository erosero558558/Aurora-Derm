# AG-009: Mobile UX phase 4 (chat/header overlap and spacing consistency)

**Status:** done  
**Completed:** 2026-02-24  
**By:** Codex

## Evidence

Implemented mobile chat viewport hardening with dynamic header offset and open-state layout lock:

- `src/apps/chat/widget-engine.js`
  - Added responsive viewport listeners (`resize`, `orientationchange`, `visualViewport.resize`).
  - Added runtime calculation of visible header bottom (`.language-bar` + `.nav`).
  - Added dynamic CSS var sync: `--chat-mobile-top-offset`.
  - Added `body.chatbot-mobile-open` toggle while chat is open in mobile viewport.
- `styles-deferred.css`
  - Added unified mobile-open override block (`@media (max-width: 900px)` + `640px`).
  - Prevents overlap by pinning chat container between top header offset and safe bottom.
  - Hides quick dock and cookie banner interaction while chat is open.
- `index.html`
  - Added equivalent critical fallback block to avoid regressions before deferred CSS is fully applied.

## Validation

Build:

- `npm run build` (OK)

Mobile regression tests:

- `npx playwright test tests/chat-mobile-layout.spec.js tests/mobile-overflow-regression.spec.js` (OK, 5 passed)
