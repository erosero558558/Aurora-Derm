# AG-186 - WhatsApp OpenClaw admin ops QA coverage

## Scope
- Extender `tests/admin.spec.js` para cubrir el panel operativo `whatsapp-openclaw-ops`.
- Validar render degradado y accion `requeue_outbox` con refresh del snapshot.

## Changes
- Se agrego un helper stateful para snapshots OpenClaw en `setupAuthenticatedAdminMocks`.
- Se anadio captura de acciones `POST` para `whatsapp-openclaw-ops`.
- Se sumaron dos pruebas Playwright:
  - render del panel con bridge degradado, fallos, holds y checkout.
  - requeue de outbox con refresh del resumen y toast de exito.

## Verification
- `TEST_REUSE_EXISTING_SERVER=1 npx playwright test tests/admin.spec.js --workers=1`
- Resultado: `25 passed`

## Notes
- No se hizo `git pull` en `main`; el worktree sigue muy sucio y `origin/main` va por delante.
- El warning de gobernanza vigente sigue siendo `public_main_sync unhealthy`.
