# CDX-004 - Package P3: descomposicion del core analitico

## Estado general

GREEN

## Metas comprometidas

- cumplida: `AnalyticsController.php` quedo como fachada HTTP/composicion y bajo de `1111` a `93` lineas.
- cumplida: funnel, retention, parseo Prometheus, normalizacion y CSV quedaron extraidos a `lib/analytics/*`.
- cumplida: contratos de `recordEvent`, `getFunnelMetrics`, `getRetentionReport` y `serviceFunnel` se preservaron.
- cumplida: consumidores laterales (`ServicePriorityController` y contrato Node del weekly report) siguieron en verde.

## Baseline -> resultado

- lineas `controllers/AnalyticsController.php`: `1111 -> 93`
- archivos nuevos: `lib/analytics/FunnelMetricsService.php`, `lib/analytics/RetentionReportService.php`, `lib/analytics/PrometheusCounterParser.php`, `lib/analytics/AnalyticsLabelNormalizer.php`, `lib/analytics/RetentionCsvExporter.php`
- gobernanza: `npm run agent:gate` en verde tras normalizar `runtime_impact` de `CDX-003` y `CDX-004`

## Contratos preservados

- `POST /funnel-event` mantiene validacion de evento, labels persistidos y respuesta `202 { ok, recorded }`
- `GET /funnel-metrics` mantiene `summary`, breakdowns, `serviceFunnel`, `surfaceFunnel`, `retention` e `idempotency`
- `GET /retention-report` mantiene contrato JSON y CSV, incluidos `meta`, `summary`, `series`, `alerts` y `filename`
- `ServicePriorityController` sigue consumiendo `serviceFunnel` con el mismo shape

## Validacion ejecutada

- `php -d xdebug.mode=coverage vendor/bin/phpunit tests/Integration/AnalyticsRetentionMetricsTest.php tests/Integration/AnalyticsServiceFunnelMetricsTest.php tests/Integration/AnalyticsRetentionReportTest.php tests/Integration/ServicePriorityControllerTest.php`
- `php tests/run-php-tests.php`
- `node --test tests-node/weekly-report-script-contract.test.js`
- `npm run agent:gate`

## Riesgo residual

- `vendor/composer/autoload_real.php` y `vendor/composer/autoload_static.php` cambiaron al ejecutar `composer dump-autoload --no-scripts` para corregir un desajuste previo del autoload local y poder correr PHPUnit.
- Sigue intacto un conflicto ajeno en `js/public-v3-shell.js`; no se toco en este paquete.

## Proximo paquete

desbloqueado
