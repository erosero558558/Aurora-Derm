# AG-083 � Frontend admin UX: collapsible sidebar + nav persistence + keyboard responsive polish

## Resumen

Se implement� un bloque grande de UX para admin enfocado en navegaci�n y productividad:

- Sidebar colapsable en desktop con persistencia (`adminSidebarCollapsed`).
- Recuperaci�n de �ltima secci�n (`adminLastSection`) y sincronizaci�n inicial de hash.
- Hardening de teclado/foco responsive (incluyendo trap de foco en sidebar con filtros de visibilidad reales).
- Ajustes visuales y de accesibilidad para estado colapsado.

## Cambios

- `admin.html`
    - Bot�n `#adminSidebarCollapse` agregado en cabecera del sidebar.
    - `title` en `nav-item` de sidebar para modo colapsado.
    - Versionado assets admin actualizado a `admin-20260226-nav9`.
- `admin.css`
    - Estilos de sidebar colapsado (`.admin-sidebar-collapsed`) y overrides responsive.
    - Estilos para `sidebar-header-actions` y `sidebar-collapse-btn`.
    - Integraci�n dark mode para nuevo bot�n.
- `src/apps/admin/index.js`
    - Persistencia de secci�n y sidebar.
    - Resoluci�n de secci�n preferida al boot.
    - Atajo `Alt+Shift+M` inteligente (desktop colapsa, compacto abre/cierra overlay).
    - Filtro robusto de focusables del sidebar para evitar focos en elementos ocultos.
- `tests/admin.spec.js`
    - Cobertura nueva para persistencia de secci�n + sidebar colapsado.
    - Cobertura nueva para atajo de teclado desktop/compacto.

## Validaci�n

- `npm run build` ?
- `npx playwright test tests/admin-navigation-responsive.spec.js -g "sidebar compacto mantiene trap"` ?
- `npm run test:frontend:qa:admin` ?
    - Resultado: `27 passed`, `1 skipped`, `0 failed`

## Resultado

Bloque AG-083 cerrado con mejoras visibles de navegaci�n admin, sin regresiones en QA admin y con cobertura de pruebas actualizada.
