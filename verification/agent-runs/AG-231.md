# AG-231 - Canonicalizar mocks auth inline en admin-queue spec

## Resumen

- Se canonicalizaron los mocks inline de `admin-auth.php` en `tests/admin-queue.spec.js`.
- El spec ahora importa el helper compartido `installLegacyAdminAuthMock` y usa un wrapper local `installQueueAdminAuthMock(page, csrfToken)`.
- Se reemplazaron `62` bloques `await page.route(/\/admin-auth\.php...)` por llamadas al wrapper.
- Durante la verificacion aparecio un rojo reproducible no relacionado con auth: el flujo `appointment-review-open-queue` ya no devuelve `queue-row-focus` al volver a cola.
- Se alineo ese assert al contrato visible actual: el ticket vuelve a estar presente en la tabla, la guia ya no lo muestra como pendiente y la actividad deja la traza `Apoyo resuelto 1602`.

## Archivos

- `tests/admin-queue.spec.js`

## Verificacion

1. `npx eslint tests/admin-queue.spec.js tests/helpers/admin-auth-mocks.js`
    - Resultado: `0` errores.
    - Advertencias heredadas: `7` warnings existentes en `tests/admin-queue.spec.js`.
2. `TEST_LOCAL_SERVER_PORT=8052 npx playwright test tests/admin-queue.spec.js -g "guia de recepcion ofrece atajos contextuales por motivo y los ejecuta" --workers=1`
    - Resultado: `1/1` passing.
3. `TEST_LOCAL_SERVER_PORT=8053 npx playwright test tests/admin-queue.spec.js --workers=1`
    - Resultado: `62/62` passing.

## Notas

- No quedaron mocks inline de `admin-auth.php` en `tests/admin-queue.spec.js`.
- `public_main_sync` seguia healthy durante el cierre de este bloque.
