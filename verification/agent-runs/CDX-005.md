# CDX-005 - Package P4: durable storage and backup core extraction

Date: 2026-03-03
Owner: Ernesto
Executor: codex

## Scorecard

- Estado general: `GREEN`
- Proximo paquete: `desbloqueado` (`P5`)

## Metas comprometidas

- `cumplida` `lib/storage.php` quedo como wrapper de compatibilidad y bajo de `1030` a `231` lineas.
- `cumplida` `lib/backup.php` quedo como wrapper de compatibilidad y bajo de `1174` a `270` lineas.
- `cumplida` paths, config, cifrado y persistencia quedaron extraidos a `lib/storage/StorageConfig.php`, `lib/storage/StorePaths.php`, `lib/storage/StoreCrypto.php` y `lib/storage/StorePersistence.php`.
- `cumplida` configuracion, receiver crypto, health y replicacion quedaron extraidos a `lib/backup/BackupConfig.php`, `lib/backup/BackupCrypto.php`, `lib/backup/BackupHealthService.php` y `lib/backup/BackupReplicationService.php`.
- `cumplida` el flujo de restore/disaster recovery quedo portable en Windows para la validacion local, sin cambiar formatos ni nombres de funciones publicas.

## Baseline -> resultado

- `lib/storage.php`: `1030` lineas -> `231`.
- `lib/backup.php`: `1174` lineas -> `270`.
- Modulos nuevos storage:
  `StorageConfig(137)`, `StorePaths(173)`, `StoreCrypto(84)`, `StorePersistence(563)`.
- Modulos nuevos backup:
  `BackupConfig(146)`, `BackupCrypto(223)`, `BackupHealthService(493)`, `BackupReplicationService(176)`.
- Superficie publica preservada:
  `storage.php` mantiene `read_store`, `write_store`, `ensure_data_file`, `create_store_backup_locked`, `data_*`.
  `backup.php` mantiene `backup_latest_status*`, `backup_create_offsite_snapshot`, `backup_upload_file`, `backup_replicate_local_file`, `backup_receiver_*`.

## Contratos preservados

- Sin cambios breaking en `store.sqlite`, `store.json`, backups `store-*.sqlite|json` ni snapshots offsite.
- Sin cambios breaking en cifrado `ENCv1:` para JSON fallback.
- Sin cambios breaking en restore CLI `bin/restore-backup.php`.
- Sin cambios breaking en `cron.php`, `HealthController`, `monitoring.php` y `backup-receiver.php` consumidores de helpers globales.

## Validacion

```powershell
php tests/test_storage_encryption.php
php tests/test_storage_backup.php
php tests/BackupReceiverTest.php
php -d xdebug.mode=coverage vendor/bin/phpunit tests/Unit/Storage/StorageSQLiteFallbackTest.php
php tests/verify_disaster_recovery_cli.php
php tests/Integration/verify_disaster_recovery.php
npm run test:php
npm run gate:prod:backend
```

Resultado observado:

- `test_storage_encryption`: `3 passed`, `0 failed`.
- `test_storage_backup`: `2 passed`, `0 failed`.
- `BackupReceiverTest`: `6 passed`, `0 failed`.
- `StorageSQLiteFallbackTest`: `3 tests`, `15 assertions`, `OK`.
- `verify_disaster_recovery_cli` e `Integration/verify_disaster_recovery`: `SUCCESS`.
- `npm run test:php`: `exit 0`.
- `npm run gate:prod:backend`: `Gate OK: despliegue validado`.

## Riesgo residual

- `composer dump-autoload --no-scripts` fue necesario para recomponer el autoload local de PHPUnit; eso dejo cambios generados en `vendor/composer/autoload_real.php` y `vendor/composer/autoload_static.php`.
- `tests/run-php-tests.php` sigue emitiendo un warning de limpieza Unix (`rm`) en algunos tests legacy, aunque la suite termina `exit 0`; no pertenece a este paquete.
- `verification/agent-metrics.json` se actualizo por comandos de gobernanza runtime y no forma parte del cambio funcional de `P4`.
