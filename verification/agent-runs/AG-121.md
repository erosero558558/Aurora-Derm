# AG-121 - Turnero sala VIII: robustez operativa modo una tecla

Fecha de cierre: 2026-02-27  
Owner: Ernesto  
Executor: codex

## Alcance ejecutado

- Robustecí el modo `1 tecla` en `src/apps/admin/index.js`:
    - Guardrail de concurrencia: evita doble ejecución mientras hay un flujo en curso.
    - Cooldown anti doble pulsación accidental (`QUEUE_ONE_TAP_COOLDOWN_MS`).
    - Telemetría operativa adicional: `one_tap_blocked` con razones `in_flight|cooldown`.
- Añadí control visible dentro de `Control de estación`:
    - Botón dinámico `Modo 1 tecla: ON/OFF` (`data-action="queue-toggle-one-tap"`).
    - Toggle por click con persistencia local.
- Mejoré guía operativa:
    - Hint dinámico en panel de atajos que refleja si el modo 1 tecla está activo o no.
    - Se mantiene atajo `Alt+Shift+E` para activar/desactivar.

## Validación ejecutada

1. `npx eslint src/apps/admin/index.js`  
   Resultado: OK.
2. `npx playwright test tests/admin-queue.spec.js`  
   Resultado: 20 passed.
3. `npx rollup -c`  
   Resultado: build OK, `admin.js` regenerado.

## Resultado operativo

- El flujo de `Numpad Enter` en modo 1 tecla ahora es más seguro para operación real.
- Se reduce riesgo de llamados duplicados por pulsaciones rápidas.
- El estado del modo queda visible y accionable sin depender de comando o atajo.
