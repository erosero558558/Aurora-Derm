# AG-254

## Scope

- Promocion de OpenClaw `web_broker` como perfil productivo canonico.
- Gates y post-deploy bloqueantes cuando OpenClaw auth no esta sano.
- Smoke live sandbox del broker con verificacion de shared session admin/turnero.
- Runbooks y checklist alineados al perfil productivo.

## Changes

- Extendi la politica de rollout admin para resolver `require_openclaw_auth` y `require_openclaw_live_smoke` por stage.
- Endureci `deploy-hosting.yml`, `post-deploy-fast.yml`, `post-deploy-gate.yml` y `prod-monitor.yml` para propagar la politica efectiva y publicar el reporte del smoke live.
- Amplie `bin/operator-auth-live-smoke.js` para `web_broker` con `callback_ok`, `shared_session_ok` y `logout_ok`.
- Actualice `VERIFICAR-DESPLIEGUE.ps1` y `MONITOR-PRODUCCION.ps1` para autoactivar `RequireOperatorAuth`.
- Alinee `docs/ADMIN-UI-ROLLOUT.md`, `docs/DEPLOYMENT.md`, `docs/DEPLOY_HOSTING_PLAYBOOK.md`, `docs/PRODUCTION_TEST_CHECKLIST.md`, `scripts/ops/admin/README.md` y `scripts/ops/prod/README.md` al corte `web_broker`.
- Deje `npm run smoke:admin:openclaw-auth:live:node` y amplie `npm run test:admin:openclaw-auth` para cubrir contratos/workflows del corte productivo.

## Verification

- `node --test tests-node/resolve-admin-rollout-policy.test.js tests-node/operator-auth-live-smoke.test.js tests-node/deploy-hosting-workflow-contract.test.js tests-node/post-deploy-fast-workflow-contract.test.js tests-node/post-deploy-gate-workflow-contract.test.js tests-node/prod-openclaw-auth-rollout-contract.test.js`
- `npm run test:admin:openclaw-auth`
