# AG-187 - Queue operator OpenClaw QA hardening

## Scope
- Endurecer `tests/queue-operator.spec.js` para los estados fragiles del login OpenClaw.
- Cubrir fallback manual, error terminal del bridge y retry con challenge nuevo.

## Changes
- Se extendio `setupOperatorAuthOperatorMocks` para soportar multiples respuestas `start` y capturar retries.
- Se agregaron pruebas Playwright para:
  - popup bloqueado con enlace manual y codigo fallback;
  - error terminal `helper_no_disponible` sin regreso al login legacy;
  - retry que genera un challenge nuevo y actualiza helper/manual code.

## Verification
- `TEST_REUSE_EXISTING_SERVER=1 npx playwright test tests/queue-operator.spec.js --workers=1`
- Resultado: `6 passed`

## Notes
- No se hizo `git pull` sobre `main`; el worktree sigue sucio y remoto va por delante.
- El warning global vigente sigue siendo `public_main_sync unhealthy`.
