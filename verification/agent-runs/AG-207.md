# AG-207 - Repair host git sync rollout and verify public_main_sync

## Summary

- Rolled out the repair escalation hardening for host git-sync recovery and promoted it to `main`.
- Verified the live `public_main_sync` signal after rollout and confirmed the remaining failure is still host/runtime state, not missing repo automation.
- Kept the repair path conservative: GitHub-hosted diagnostics can probe connectivity, and the self-hosted fallback is opt-in and explicitly observable.

## Shipped changes

- Commit `615fc2f2` added repair escalation controls in `.github/workflows/repair-git-sync.yml` and updated `docs/PUBLIC_MAIN_UPDATE_RUNBOOK.md`.
- Commit `a628e848` hydrated the transport preflight reason from workflow outcome in `.github/workflows/deploy-hosting.yml`, keeping transport failures diagnosable when the preflight artifact is absent.

## Verification

- `node --test tests-node/deploy-hosting-workflow-contract.test.js`
- `gh run list --workflow repair-git-sync.yml --limit 5`
- `gh run list --workflow diagnose-host-connectivity.yml --limit 5`
- `gh run list --workflow deploy-frontend-selfhosted.yml --limit 5`
- `node agent-orchestrator.js jobs verify public_main_sync --json`

## Key results

- Repair workflow run `22986675013` (`2026-03-12T04:31:14Z`) completed with `failure`, which is expected for the current host-side blocker because the repo-side repair path can only escalate, not heal unreachable transport.
- Connectivity diagnose run `22987087004` (`2026-03-12T04:49:17Z`) completed with `success`, confirming the diagnostic workflow executed and produced probe evidence.
- Self-hosted fallback run `22987087006` (`2026-03-12T04:49:17Z`) was cancelled intentionally after remaining queued, so we would not leave a stale delayed deploy armed in GitHub Actions.
- `public_main_sync` verification at `2026-03-12T05:10:02Z` remained unhealthy with:
    - `state=failed`
    - `failure_reason=working_tree_dirty`
    - `telemetry_gap=true`
    - `current_head=''`
    - `remote_head=''`

## Conclusion

- AG-207 is complete as a repo/workflow rollout and verification task.
- The remaining blocker is live infrastructure state on the host or runner path, not missing repair workflow logic in the repository.
