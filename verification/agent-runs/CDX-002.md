# CDX-002 - Package P1: Sentry evidence normalization and readiness scorecard

Date: 2026-03-02
Owner: Ernesto
Executor: codex

## Scorecard

- Estado general: `GREEN`
- Proximo paquete: `desbloqueado` (`P2`)

## Metas comprometidas

- `cumplida` `npm run verify:sentry:events` ahora siempre deja `verification/runtime/sentry-events-last.json`, incluso cuando faltan credenciales o falla la API.
- `cumplida` `bin/prod-readiness-summary.js` consume evidencia Sentry remota/local antes de clasificar `PM-SENTRY-001`.
- `cumplida` el workflow manual `.github/workflows/sentry-events-verify.yml` ya no bloquea antes de que exista el artefacto `sentry-events-report`.
- `cumplida` la plantilla oficial de cierre por paquete queda fijada en esta misma evidencia.

## Baseline -> resultado

- Artefacto Sentry:
  antes: el script fallaba sin dejar JSON utilizable cuando faltaban `SENTRY_AUTH_TOKEN` o `SENTRY_ORG`.
  resultado: el artefacto expone `status=needs_configuration`, `failureReason.code=missing_env` y `actionRequired`.
- Readiness summary:
  antes: `PM-SENTRY-001` quedaba como pendiente generico.
  resultado: el summary publica seccion `Sentry Evidence`, marca el origen (`remote_artifact` o `local`) y genera notas/acciones desde la evidencia real.
- Workflow manual:
  antes: el job podia fallar en prevalidacion sin producir artefacto.
  resultado: el script es la fuente unica del diagnostico y el upload siempre tiene un archivo que publicar.
- Cobertura:
  antes: no habia pruebas dedicadas para este flujo.
  resultado: se agregaron contratos Node para script, workflow y summary; `npm run agent:test` quedo en `243/243` pass.

## Contratos preservados

- Sin cambios breaking en `/health`.
- Sin cambios breaking en el comando `npm run verify:sentry:events`.
- Sin cambios breaking en el nombre del workflow manual `Sentry Events Verify (Manual)`.
- El artefacto canonico sigue en `verification/runtime/sentry-events-last.json`; solo se agregaron campos aditivos.

## Validacion

```powershell
node bin/verify-sentry-events.js
node --test tests-node/verify-sentry-events-script.test.js tests-node/prod-readiness-summary-sentry.test.js tests-node/sentry-events-workflow-contract.test.js
npm run agent:test
node bin/prod-readiness-summary.js
```

Resultado observado:

- `node bin/verify-sentry-events.js` -> `FAIL` esperado con artefacto utilizable y accionable (`missing_env`).
- `node --test ...` -> `4 passed`.
- `npm run agent:test` -> `243 passed`, `0 failed`.
- `node bin/prod-readiness-summary.js` -> seccion `Sentry Evidence` visible y `PM-SENTRY-001` alimentado desde evidencia local.

## Riesgo residual

- La evidencia operativa final de Sentry sigue `pending_external` hasta configurar `SENTRY_AUTH_TOKEN` y `SENTRY_ORG` o ejecutar el workflow manual con esos secretos.
- El estado global de readiness sigue `RED` por causas externas a este paquete: falla reciente de `CI`, falla reciente de `Deploy Hosting` e issue abierto `[ALERTA PROD] Post-Deploy Fast Lane fallando`.

## Plantilla oficial de scorecard

Usar esta estructura para cada paquete grande:

````md
# <TASK_ID> - <Package title>

Date: <YYYY-MM-DD>
Owner: <owner>
Executor: <executor>

## Scorecard

- Estado general: `GREEN | YELLOW | RED`
- Proximo paquete: `desbloqueado | bloqueado`

## Metas comprometidas

- `cumplida | no cumplida` <meta 1>
- `cumplida | no cumplida` <meta 2>

## Baseline -> resultado

- <baseline breve> -> <resultado medible>
- <baseline breve> -> <resultado medible>

## Contratos preservados

- <contrato 1>
- <contrato 2>

## Validacion

```powershell
<comandos ejecutados>
```

Resultado observado:

- <resultado 1>
- <resultado 2>

## Riesgo residual

- <riesgo 1>
- <riesgo 2>
````
