# AG-105 - Turnero sala I: triage SLA con filtros y busqueda en admin

Fecha: 2026-02-26
Owner: Ernesto
Executor: codex

## Cambios implementados

- `src/apps/admin/modules/queue.js`
    - Triage operativo en tiempo real: filtros (`all`, `waiting`, `called`, `sla_risk`, `appointments`, `walk_in`) y busqueda por ticket/iniciales/ultimos 4.
    - Modelo de vista con prioridad de cola (`appt_overdue`, `appt_current`, `walk_in`) y ranking por estado.
    - Calculo de espera por ticket y deteccion de riesgo SLA (`>= 20m`) para tickets en espera.
    - Resumen dinamico de triage (visibles/total/en espera/en riesgo) y controles activos con estado visual.
    - Render de tabla con indicadores de riesgo SLA y etiqueta de espera por fila.
- `admin.css`
    - Estilos para toolbar de triage, filtros activos, busqueda y resaltado de filas en riesgo SLA.
- `tests/admin-queue.spec.js`
    - Nuevo escenario E2E para filtros SLA y busqueda de ticket.

## Validacion

- `npx eslint src/apps/admin/modules/queue.js tests/admin-queue.spec.js` -> OK
- `npm run build` -> OK
- `npx playwright test tests/admin-queue.spec.js tests/queue-kiosk.spec.js tests/queue-display.spec.js` -> OK (4/4)

## Resultado

El operador ahora puede priorizar la cola por riesgo SLA y localizar turnos rapidamente desde Admin, reduciendo tiempo de triage y errores de despacho en sala.
