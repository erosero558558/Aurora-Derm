# AG-234 - Canonicalizar mock OpenClaw auth en admin login spec

## Resumen

- Se extrajo el mock especial de `admin-auth.php` para `openclaw_chatgpt` a `tests/helpers/admin-auth-mocks.js`.
- El helper compartido ahora expone `buildOpenClawAdminChallenge`, `buildOpenClawAdminAuthPayload` e `installOpenClawAdminAuthMock`.
- `tests/admin-openclaw-login.spec.js` dejo de mantener una maquina de estados inline y ahora consume el helper canonico sin cambiar el contrato de polling ni los estados terminales.

## Archivos

- `tests/helpers/admin-auth-mocks.js`
- `tests/admin-openclaw-login.spec.js`

## Verificacion

1. `npx eslint tests/helpers/admin-auth-mocks.js tests/admin-openclaw-login.spec.js`
    - Resultado: `0` errores, `0` warnings.
2. `TEST_LOCAL_SERVER_PORT=8056 npx playwright test tests/admin-openclaw-login.spec.js --workers=1`
    - Resultado: `3/3` passing.

## Notas

- Este bloque deja centralizados tanto el mock legacy como el mock OpenClaw de `admin-auth.php` para Playwright.
- El inline restante de `page.route(/\/admin-auth\.php...)` esta en `tests/admin.spec.js` y corresponde al flujo stateful de login legacy (`status/login/login-2fa/logout`), que queda como siguiente bloque natural.
- `public_main_sync` seguia healthy durante el cierre de este bloque.
