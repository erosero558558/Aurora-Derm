# AG-232 - Canonicalizar mocks auth inline en admin spec

## Resumen

- `tests/admin.spec.js` ahora consume el helper compartido `tests/helpers/admin-auth-mocks.js`.
- `setupAuthenticatedAdminMocks()` dejó de declarar su propio `page.route(/\/admin-auth\.php...)` y usa `installLegacyAdminAuthMock`.
- `setupLoginAdminMocks()` mantiene la state machine local de `status/login/login-2fa/logout`, pero ahora construye sus respuestas con `buildLegacyAdminAuthPayload` para devolver el shape legacy canónico en todas las ramas.

## Archivos

- `tests/admin.spec.js`

## Verificacion

1. `npx eslint tests/admin.spec.js tests/helpers/admin-auth-mocks.js`
    - Resultado: `0` errores, `0` warnings.
2. `TEST_LOCAL_SERVER_PORT=8054 npx playwright test tests/admin.spec.js --workers=1`
    - Resultado: `22/22` passing.

## Notas

- No se tocó el flujo especial `openclaw_chatgpt`; este bloque solo alineó el contrato legacy de login/admin en el smoke general.
- `public_main_sync` seguía healthy durante el cierre del bloque.
