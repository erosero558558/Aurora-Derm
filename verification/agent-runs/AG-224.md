# AG-224 - Align production checklist public smoke to current public V6 contract

## Summary

- Replaced the legacy public smoke assumptions in `tests/checklist-production.spec.js` with checks that match the current `public-v6` routing and maintenance flow.
- Updated the production pre-check to validate the localized entrypoints and assets that actually ship today:
    - `es/index.html`
    - `en/index.html`
    - `es/servicios/index.html`
    - `es/telemedicina/index.html`
    - `es/legal/**`
    - `js/public-v6-shell.js`
    - `_astro`
- Reworked the public checklist coverage so it now validates:
    - `/es/` exposes the booking maintenance status and telemedicine CTA.
    - `/es/telemedicina/` preserves the fallback route back to `/es/servicios/`.
    - The fast-contact WhatsApp CTA remains present in the header.
    - The removed legacy booking shell does not reappear (`appointmentForm`, `paymentModal`, `chatbotWidget`).
- Expanded the admin login-failed title matcher to cover the current admin shell title (`Nucleo Interno de Consultorio - Piel en Armonia`).

## Reproduced issue

- `npx playwright test tests/checklist-production.spec.js --grep "3\\. Cita Pública|5\\. Callback|6\\. Resenas" --workers=1`
- Original failures came from expectations that no longer match the live `public-v6` contract:
    - `#appointmentForm` no longer exists on the public site.
    - `/index.html` returned `404`.
    - The old callback phone inputs are no longer present.
- A subsequent full-file run also exposed drift in the admin login-failed test, which still expected the previous admin title pattern.

## Verification

- `npx eslint tests/checklist-production.spec.js`
- `TEST_LOCAL_SERVER_PORT=8033 npx playwright test tests/checklist-production.spec.js --workers=1`

## Outcome

- `10/10` executable Playwright tests passing in `tests/checklist-production.spec.js`.
- `1` test remains intentionally skipped when `PIELARMONIA_ADMIN_PASSWORD` is not configured.
- ESLint completed without errors; remaining output is warnings only from existing Playwright lint rules in this spec.
