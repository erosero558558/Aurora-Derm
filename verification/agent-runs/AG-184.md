# AG-184 - Operator Auth live smoke runner

Fecha: 2026-03-12
Owner: `codex_backend_ops`

## Objetivo del bloque

Agregar un smoke reusable backend-only para validar el login OpenClaw real de
`admin-auth.php` contra el helper `operator-auth-bridge`, sin depender solo de
mocks Playwright.

## Cambios

- Nuevo runner CLI: `bin/operator-auth-live-smoke.js`
- Nueva cobertura Node: `tests-node/operator-auth-live-smoke.test.js`

## Verificaciones automatizadas

```text
node --check bin/operator-auth-live-smoke.js
node --check tests-node/operator-auth-live-smoke.test.js
node --test tests-node/operator-auth-live-smoke.test.js
php tests/test_operator_auth.php
node --test tests-node/operator-auth-bridge.test.js tests-node/operator-auth-live-smoke.test.js
```

Resultado:

```text
2/2 tests nuevos Node OK
4/4 tests PHP operator auth OK
6/6 tests Node bridge+live smoke OK
```

## Smoke local real

Comandos ejecutados:

```text
node bin/operator-auth-live-smoke.js --preflight-only --request-timeout-ms=4000 --poll-timeout-ms=4000 --json-out=verification/operator-auth-live-smoke/preflight-last.json
node bin/operator-auth-live-smoke.js --request-timeout-ms=6000 --poll-timeout-ms=15000 --json-out=verification/operator-auth-live-smoke/full-last.json
```

Resultado observado:

- `http://127.0.0.1:8011/admin-auth.php?action=status` respondio `200` con `mode=openclaw_chatgpt`.
- `http://127.0.0.1:4173/health` respondio `service=operator-auth-bridge`.
- `admin-auth.php?action=start` devolvio challenge `pending` con `helperUrl` correcto.
- El flujo completo resolvio identidad real `javier.rosero94@gmail.com`.
- `admin-auth.php?action=status` paso a `autenticado` con `csrfTokenPresent=true`.
- `admin-auth.php?action=logout` cerro la sesion correctamente.

Artefactos:

- `verification/operator-auth-live-smoke/preflight-last.json`
- `verification/operator-auth-live-smoke/full-last.json`
