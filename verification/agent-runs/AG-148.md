# AG-148 - Production verify sony_v3 live

Date: 2026-03-02
Owner: Ernesto
Executor: codex

## Goal

Verify the live production site serves the admin runtime correctly after the manual VPS/OpenClaw recovery and feature flag activation.

## Checks executed

### Live endpoint checks

- `https://pielarmonia.com/api.php?resource=features`
  - `admin_sony_ui: true`
  - `admin_sony_ui_v3: true`
- `https://pielarmonia.com/admin.html`
  - contains `admin-v3.css`
- `https://pielarmonia.com/admin.html?admin_ui=sony_v3`
  - serves the v3 asset path correctly

### Browser runtime smoke against production

Command:

```powershell
$env:TEST_BASE_URL='https://pielarmonia.com'
npx playwright test tests/admin-ui-runtime-smoke.spec.js tests/admin-v3-canary-runtime.spec.js --workers=1
```

Result:

- `4 passed`

Validated behaviors:

- runtime variant resolution by query/reset/feature flags
- hardened admin CSP meta present
- `sony_v3` shell boots with isolated assets
- operational navigation works across appointments, callbacks, reviews and availability

## Conclusion

Production admin `sony_v3` is live and passes the existing runtime smoke suite against the public domain.
