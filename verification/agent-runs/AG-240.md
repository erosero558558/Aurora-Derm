# AG-240 - Consolidar mocks de apps operativas desktop en admin queue spec

## Resumen

- Se extrajeron helpers locales en `tests/admin-queue.spec.js` para consolidar
  el armado de `queueSurfaceStatus`, instancias desktop del operador y el
  wrapper de mocks `api.php` del bloque de apps operativas.
- Se migraron a esos helpers el hub de apps operativas y los cuatro escenarios
  de desktop operador / auto-update.
- El bloque quedó sin `page.route(/\/api\.php...)` inline en esa zona del spec.

## Archivos

- `tests/admin-queue.spec.js`

## Verificación

- `npx eslint tests/admin-queue.spec.js`
    - Resultado: `0` errores, `7` warnings heredados.
- `npx playwright test tests/admin-queue.spec.js -g "queue muestra hub de apps operativas|admin muestra dos operadores Windows por estacion|admin mantiene visible una desktop operador en configuracion local|admin expone intento y causa|admin mantiene visible launchMode|admin muestra progreso de auto-update" --workers=1`
    - Resultado: `6/6` en verde.
- `npx playwright test tests/admin-queue.spec.js --workers=1`
    - Resultado: `84/84` en verde.

## Notas

- No se tocaron otros frentes activos ni mocks inline fuera de este clúster.
