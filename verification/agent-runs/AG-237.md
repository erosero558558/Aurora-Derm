# AG-237 - Consolidar mocks del piloto web en admin queue spec

## Resumen

- extraje builders reutilizables para el perfil clinico, el estado idle, el health y el estado de superficies del piloto web dentro de `tests/admin-queue.spec.js`
- agregue `installQueuePilotApiMocks()` para evitar payloads repetidos y hacer el setup del piloto mas consistente
- reescribi el escenario del piloto listo para usar el helper consolidado sin cambiar el comportamiento esperado

## Verificacion

- `npx eslint tests/admin-queue.spec.js`
- `TEST_REUSE_EXISTING_SERVER=1 npx playwright test tests/admin-queue.spec.js --grep "piloto web" --workers=1`
- `TEST_REUSE_EXISTING_SERVER=1 npx playwright test tests/admin-queue.spec.js --grep "queue advierte en readiness si /health reporta otra clínica activa para el piloto" --workers=1`
