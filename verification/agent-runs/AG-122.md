# AG-122 - Turnero sala IX: TV premium con anuncio claro y privacidad estricta

Fecha de cierre: 2026-02-27  
Owner: Ernesto  
Executor: codex

## Alcance ejecutado

- `src/apps/queue-display/index.js`
    - Agregué banner principal de llamado en TV (inyectado por JS) para lectura a distancia: `ticket + iniciales + consultorio`.
    - Implementé normalización estricta de privacidad en TV:
        - `normalizePatientInitialsForDisplay(...)` para evitar nombre completo en pantalla.
        - `normalizeTicketCodeForDisplay(...)` para sanitizar código mostrado.
    - Integré selección del llamado principal activo por timestamp (`calledAt`) para anuncio consistente.
    - Añadí estilos inline del banner con variante `is-live/is-idle` y soporte `prefers-reduced-motion`.
    - Añadí telemetría UI aditiva `announcement_update` en `piel:queue-ops`.

## Validación ejecutada

1. `npx eslint src/apps/queue-display/index.js`  
   Resultado: OK.
2. `npx playwright test tests/queue-display.spec.js`  
   Resultado: 7 passed.
3. `npx rollup -c`  
   Resultado: build OK, `js/queue-display.js` regenerado.

## Resultado operativo

- La TV ahora prioriza claridad visual del llamado activo sin exponer datos sensibles.
- Si llega data no normalizada desde backend, la vista pública sigue mostrando solo formato seguro.
- Se mantiene watchdog/reintentos/campanilla existentes sin regresiones en la suite de display.
