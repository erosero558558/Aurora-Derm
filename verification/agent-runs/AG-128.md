# AG-128 — Turnero sala: bloque QA Modo 1 tecla + persistencia por estación

## Fecha

- 2026-02-28

## Objetivo

- Consolidar el flujo `Modo 1 tecla` para estaciones bloqueadas (C1/C2), verificando comportamiento real y persistencia tras recarga.

## Cambios aplicados

- `tests/admin-queue.spec.js`
    - Se agregaron 2 pruebas E2E nuevas:
        - `modo 1 tecla provisionado completa ticket activo y llama siguiente en consultorio bloqueado`
        - `modo 1 tecla sin ticket activo solo llama siguiente (sin completar)`
    - Cobertura añadida:
        - provisión por URL con `one_tap=1`,
        - ejecución de `NumpadEnter` en modo 1 tecla con lock por consultorio,
        - validación de secuencia `PATCH completar` + `POST call-next`,
        - validación de caso sin ticket activo (solo `call-next`, sin `PATCH`),
        - persistencia de estado `Modo 1 tecla` tras recarga.
- `docs/RUNBOOK_TURNERO_SALA.md`
    - Se documentó provisión opcional con `one_tap=1` por estación.
    - Se agregó `queueOneTapAdvance` en estado persistido local.
    - Se reforzó checklist de recuperación para validar `Modo 1 tecla` (`ON/OFF`).

## Validación ejecutada

- `npx eslint tests/admin-queue.spec.js`
    - Resultado: OK
- `npx playwright test tests/admin-queue.spec.js -g "modo 1 tecla"`
    - Resultado: 2/2 OK
- `npx playwright test tests/admin-queue.spec.js`
    - Resultado: 22/22 OK

## Resultado

- `Modo 1 tecla` queda cubierto por QA automatizado en escenarios críticos de operación por estación.
- Runbook actualizado con provisión y recuperación explícita para operación en sala.
