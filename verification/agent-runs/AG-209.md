# AG-209 - Deploy-hosting transport evidence hardening

## Summary

- Replaced the inline transport preflight writers in `deploy-hosting.yml` with the versioned helper `bin/write-transport-preflight.js`.
- Added `bin/write-admin-rollout-placeholder-report.js` so early transport failures still upload `verification/last-admin-ui-rollout-gate-deploy-hosting.json`.
- Kept the transport incident lane explicit in `deploy-hosting.yml`, with workflow contract coverage for create/close behavior.
- Documented the expected artifacts and incident semantics in `docs/PUBLIC_MAIN_UPDATE_RUNBOOK.md`.

## Verification

- `node --test tests-node/write-transport-preflight-script.test.js tests-node/write-admin-rollout-placeholder-script.test.js tests-node/deploy-hosting-workflow-contract.test.js`

## Expected runtime outcome

- `deploy-hosting.yml` should fail only for the canonical transport blocker (`runner_tcp_unreachable`) when the runner cannot reach the host.
- Early-failure runs should still upload:
    - `.public-cutover/transport-preflight.json`
    - `verification/last-admin-ui-rollout-gate-deploy-hosting.json`
- The dedicated issue `[ALERTA PROD] Deploy Hosting transporte bloqueado desde GitHub Runner` should open on `runner_tcp_unreachable` and close after a later preflight returns `ok`.
