---
const { timeline } = Astro.props;
---

<section class="service-timeline-v2" data-service-timeline>
    <p>{timeline.eyebrow}</p>
    <div class="service-timeline-v2__steps">
        {
            timeline.steps.map((step, index) => (
                <article>
                    <span>{`0${index + 1}`}</span>
                    <h3>{step}</h3>
                </article>
            ))
        }
    </div>
</section>
