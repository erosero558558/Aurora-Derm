---
const { families = [] } = Astro.props;
---

<nav class="family-tabs-v2" data-family-tabs>
    {
        families.map((family) => (
            <a href={`#family-${family.id}`} class="family-tabs-v2__link" data-family-tab={family.id}>{family.label}</a>
        ))
    }
</nav>
