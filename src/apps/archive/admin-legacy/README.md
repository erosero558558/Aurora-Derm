# Admin Legacy Source Archive

Archivo del runtime legacy del admin previo al entrypoint canonico actual.

Incluye:

- `legacy-index.js`
- `modules/**`
- `utils.js`

Se conserva solo como referencia historica. El runtime activo vive en
`src/apps/admin/index.js` y carga `src/apps/admin-v3/**`.
