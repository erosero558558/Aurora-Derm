# Admin V2 Source Archive

Archivo historico de la variante `sony_v2` del admin.

No forma parte del shell `admin.html`, del bundle `admin.js` ni del gate
operativo actual. El admin canonico arranca en `sony_v3` y el rollback se
hace por `revert + deploy`.
